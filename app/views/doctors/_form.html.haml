.container
  = simple_form_for(@doctor, html: { class: 'form-horizontal'} ) do |f|
    = f.error_notification

    .form-inputs
      = f.input :name
      = f.input :hourly_rate
      = f.input :hours_per_week

    .form-actions
      = f.button :submit
